# Environment variables for Adsphere

# Example: Your NEXT_PUBLIC API keys (accessible to the browser)
NEXT_PUBLIC_SITE_NAME=Adsphere
NEXT_PUBLIC_API_URL=https://api.adsphere.com

# Example: Private server-side secrets (not exposed to the client)
DATABASE_URL=postgresql://username:password@localhost:5432/adsphere
JWT_SECRET=your_super_secret_jwt_key
NEXTAUTH_SECRET=your_super_secret_nextauth_key

# Deployment environment
NODE_ENV=development
